{
  "name": "chromewhale",
  "version": "0.10.0",
  "private": true,
  "description": "Chromewhale: the Codewhale Chrome extension. Side-panel chat over the local runtime API, with browser tools the model executes in the tab you are looking at.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "check": "npm test"
  }
}
