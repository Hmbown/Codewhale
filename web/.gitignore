node_modules
.next
.open-next
.wrangler
.env
.env.local
.env.*.local
*.log
.DS_Store
next-env.d.ts
.vercel
tsconfig.tsbuildinfo
.dev.vars

# Derived at build/test time from CHANGELOG.md and docs/INSTALL.md. Not
# tracked: two PRs that each regenerated a committed copy against their own
# base merged to a stale file and reddened main (2026-09-26). `npm run
# prebuild`, `npm run dev` and the vitest global setup write them.
lib/changelog.generated.ts
lib/install-guide.generated.ts
